package fashiony;

//Using enum to hold fixed list of actions to pass in to Operation class
//in order to make classification easier
public enum OpType {
    TEMP_ADD,
    ADD_TAG,
    REMOVE_TAG,
    EDIT_NOTE,
    DELETE_VERSION
}
